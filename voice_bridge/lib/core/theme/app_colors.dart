import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF89CFF0);   // Soft Blue (Calming & Soothing)
  static const Color secondary = Color(0xFF98FB98); // Pastel Green (Comforting for autistic children,)
  static const Color background = Color(0xFFF5F5F5); // Light Gray (Neutral)
  static const Color textPrimary = Color(0xFF212121); // Dark Gray (Good Readability)
  static const Color textSecondary = Color(0xFF757575); // Light Gray
}
